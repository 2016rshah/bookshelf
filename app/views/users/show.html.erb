<div class="index">
  <div class="row">
    <div class = "columns small-12">
    	<% if current_user == @user %>
    		<h1>Welcome to your bookshelf</h1>
    	<% else %>
      	<h1>Welcome to <%= @user.email %>'s bookshelf</h1>
      <% end %>
    </div>
  </div>
	<% if current_user == @user %>
		<div class="row">
	    <div class="new-book twelve columns">
	      <%= link_to 'New book', new_book_path, :class=>"button button-primary" %>
	    </div>
	  </div>
  <% end %>
  <div class = "row">
    <div class="twelve columns">
    <% @books.each_slice(4) do |books| %>
      <div class = "row">
        <% books.each do |book| %>
          <div class = "book three columns">
            <%= link_to image_tag("http://covers.openlibrary.org/b/isbn/#{book.isbn}-M.jpg"), book %>
            <p>
              <ul class='links'>
                <li><%= link_to 'Show', book %></li>
                <% if current_user == @user %>
                	<li><%= link_to 'Edit', edit_book_path(book) %></li>
                	<li><%= link_to 'Destroy', book, method: :delete, data: {confirm: 'You sure?'} %></li>
                <% end %>
              </ul>
            </p>
          </div>
        <% end %>
      </div>
    <% end %>
    </div>
  </div>
</div>